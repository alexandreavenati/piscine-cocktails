{% include 'partial/_header.html.twig' %}

<h1>Cocktails récents</h1>

<section class="cocktail-list">
	{# Boucle pour afficher les cocktails du tableau "cocktail" #}
	{% for cocktail in cocktails %}
		<article class="cocktail-card">
			<img src="{{ cocktail.image }}" alt="{{ cocktail.nom }}">
			<div class="cocktail-info">
				<h2><a href="{{ path('cocktail-show', {'id': cocktail.id}) }}">{{ cocktail.nom }}</a></h2>
				<p>{{ cocktail.description }}</p>
				<ul>
					{% for ingredient in cocktail.ingredients %}
						<li>{{ ingredient }}</li>
					{% endfor %}
				</ul>
				<p class="date">Date de création: {{ cocktail.date_creation | date('d/m/Y') }}</p>
			</div>
		</article>
	{% endfor %}
</section>

{% include 'partial/_footer.html.twig' %}

